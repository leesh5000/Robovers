# Backend Environment Variables
DATABASE_URL=postgresql://robovers:robovers123@postgres:5432/robovers?schema=public
REDIS_URL=redis://redis:6379

# Application
NODE_ENV=production
BACKEND_PORT=4010
FRONTEND_PORT=3000

# JWT
JWT_SECRET=robovers-prod-jwt-secret-key-2024-change-this-in-production
JWT_ACCESS_TOKEN_EXPIRATION=15m
JWT_REFRESH_TOKEN_EXPIRATION=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# API URL (for frontend)
NEXT_PUBLIC_API_URL=http://localhost:4010/api

# PostgreSQL
POSTGRES_USER=robovers
POSTGRES_PASSWORD=robovers123
POSTGRES_DB=robovers

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@robovers.com
PGADMIN_DEFAULT_PASSWORD=admin123

# Optional: Email Service (for future implementation)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password